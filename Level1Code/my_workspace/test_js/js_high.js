// alert('hello world');
$(document).ready(function() {
     // Stuff to do as soon as the DOM is ready
     $('#btn').click(function(){
         var p1=$('#text').val();
         $('#p').html(p1);

     });
});
